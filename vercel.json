{
  "version": 2,
  "buildCommand": "cd lcopilot/frontend && npm install && npm run build",
  "outputDirectory": "lcopilot/frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
